# Read registry data

Return the in-memory \`responses\` data frame, if present.

## Usage

``` r
tar_read_data()
```

## Value

A data frame or NULL if not available.

## Examples

``` r
# \donttest{
if(interactive()){
 #EXAMPLE1
 }
# }
```
